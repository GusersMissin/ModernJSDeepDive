//Ternary Conditional Operator
var score = 60;
var result = score >= 60 ? 'pass' : 'fail';

console.log(result);

score = 20;
result = score >= 60 ? 'pass' : 'fail';

console.log(result);

/*
Same with
if(score >= 60){
    result = 'pass';
} else {
    result = 'fail';
}
*/